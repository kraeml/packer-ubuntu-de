---
  - name: "Check box {{box_developer}}/{{box_lts}} is in version {{box_version}}"
    # Don't remove this task, because if base box is missing geerlingguy/ubuntu1804
    command: "vagrant box add --box-version {{box_version}} {{box_developer}}/{{box_lts}}"
    register: box_info
    ignore_errors: yes
    changed_when: False
    when: box_developer == "geerlingguy"

  - name: "Display box info"
    debug:
      var: box_info
      verbosity: 1

  - name: "Display box path variable"
    debug:
      var: box_ovf_path
      verbosity: 1

  - name: "Test box exists"
    command: "ls -ld {{box_ovf_path}}"
    args:
      creates: "{{box_ovf_path}}"

  - name: Ensure virtualbox-ovf directory exists
    file:
      path: "./virtualbox-ovf/pre_{{BASE}}"
      src: "{{box_ovf_path}}"
      state: link

  - block:
    - name: first file check
      stat:
        path: "{{box_ovf_path}}/version"
        get_checksum: yes
      register: test_file_check_1

    - name: next check
      stat:
        path: "builds/version-{{box_lts}}"
        get_checksum: yes
      register: test_file_check_2

    - debug:
        var: test_file_check_1
        verbosity: 1
    - debug:
        var: test_file_check_2
        verbosity: 1

    - name: Block run only if file has changes
      command: /bin/false
      when: test_file_check_1.stat.checksum != test_file_check_2.stat.checksum
    when: box_developer != "geerlingguy"
